"use strict"; // Sinon le prof se fache XD


$( document ).ready(function() {
    // Exo : 3) maVar is not defined

    alert("diff var = " + maVar2 - maVar1);
    console.log( "Page ready!" );
    alert("Number Zero (externe JS version)");
});